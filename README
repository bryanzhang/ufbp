用于在网络多机器之间传送数据, 传送方式是pull不是push,适合大文件传送。
将文件分成pack发送，服务器端会统筹需要发送的pack,最多需要它的发出来,pack的传送是不可靠的,客户端需要发送一段时间内的确认。
客户端需要发送请求,这个request包，带一个唯一id,request id由8个字节构成(时间4个字节,主机id 2字节,序列号 2字节),这样尽管只有一个传送端口，仍然可以发起多个请求。还有请求的uri(类似http了)
request id
request包需要可靠传输,和后面客户端回送的已传确认一样，需要一个确认包和序列机制保证可靠性。具体是如果50ms接收不到确认包就会重发,如果5秒钟收不到则放弃。同样服务器端如果超过5秒钟没有收到客户端的回传确认也可能会放弃传送。response包格式如下:
code （404，200，500）
requestid
lastRecvPack (最近的回传确认包序列号)
filelength
packnum
data (1024Bytes)
先在单机做，如果性能与nginx差不多，再做一次整体测试。服务器端和客户端都需要保持各个连接的状态。
